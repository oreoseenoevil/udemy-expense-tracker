// React is a Javascript Library for building user interfaces
// Reack makes building complex, interactive, and reactive user interfaces simpler
// React is all about components

// Components are building block of application UI. These components are split up the entire UI into small independent and reusable pieces. then it renders each of these components independently without affecting each other.


// JSX is a javascript xml, where we can write HTML structures in same file that contains javascript. Browser can only read Javascript object but jsx is not a regular object and to read JS we need transformers like babel to pass it to the browsers.